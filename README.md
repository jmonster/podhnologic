# the `g` is silent.

```sh
pnpx github:jmonster/music-monstger --input "/path/to/input" --output "/path/to/output" --pod # defaults to aac

pnpx github:jmonster/music-monstger --input "/path/to/input" --output "/path/to/output" --codec alac # alac flac aac ogg wav mp3
```
